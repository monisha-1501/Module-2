# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```
a = int(input())
b = int(input())

f = lambda a, b: a + b

print(f(a, b))
```

## Output
<img width="1324" height="314" alt="{E9AEE8F1-1E9C-443E-812E-54F524F58B3F}" src="https://github.com/user-attachments/assets/812c650f-8eb8-4bf4-aebb-dc468bbe4e14" />

## Result
The program successfully uses a lambda function to add two integers entered by the user and prints the sum.
